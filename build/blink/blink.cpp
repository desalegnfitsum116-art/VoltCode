// Generated by voltc. Do not edit.

#include <Arduino.h>
#include <stdint.h>
#include <stdbool.h>
#include <string.h>

typedef struct {
    uint8_t pin;
    uint8_t mode;
} VoltDigitalPin;
typedef struct {
    uint8_t _unused;
} VoltArduino;

static VoltDigitalPin* volt_digitalpin_init(VoltDigitalPin* self, int16_t pin, int16_t mode) {
    self->pin = (uint8_t)pin;
    self->mode = (uint8_t)mode;
    pinMode(pin, mode);
    return self;
}

static VoltArduino* volt_arduino_init(VoltArduino* self) {
    (void)self;
    return self;
}

VoltDigitalPin* led;
VoltArduino* board;
static VoltDigitalPin __volt_digitalpin_0;
static VoltArduino __volt_arduino_1;


void setup() {
    led = volt_digitalpin_init(&__volt_digitalpin_0, 13, OUTPUT);
    board = volt_arduino_init(&__volt_arduino_1);
}

void loop() {
    while (true) {
        digitalWrite(led->pin, 1);
        delay(1000);
        digitalWrite(led->pin, 0);
        delay(1000);
    }
}