// Generated by voltc. Do not edit.

#include <Arduino.h>
#include <Servo.h>
#include <stdint.h>
#include <stdbool.h>
#include <string.h>

typedef struct {
    uint8_t _unused;
} VoltArduino;

static Servo* volt_servo_init(Servo* self, int16_t pin) {
    self->attach(pin);
    return self;
}

static VoltArduino* volt_arduino_init(VoltArduino* self) {
    (void)self;
    return self;
}

VoltArduino* board;
Servo* myServo;
int16_t pos = 0;
static VoltArduino __volt_arduino_0;
static Servo __volt_servo_1;


void sweep(int16_t start, int16_t end, int16_t step);

void sweep(int16_t start, int16_t end, int16_t step) {
    int16_t angle;
    angle = start;
    while ((angle <= end)) {
        myServo->write(angle);
        delay(15);
        angle = (angle + step);
    }
}

void setup() {
    board = volt_arduino_init(&__volt_arduino_0);
    myServo = volt_servo_init(&__volt_servo_1, 9);
}

void loop() {
    while (true) {
        sweep(0, 180, 1);
        sweep(180, 0, (-1));
    }
}